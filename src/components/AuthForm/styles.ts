import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        marginTop: 48,
    },

    containerPosition: {
        alignContent: "center",
        marginTop: 0,
        paddingBottom: 48,
    },

    logo: {
        alignSelf: 'center',
    },
});